<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.johal.companysystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".managerHomePage"
            android:label="@string/title_activity_admin_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditProfile" />
        <activity android:name=".AddEmployee" />
        <activity android:name=".EmployeeEdit" />
        <activity android:name=".CompletedJobs" />
        <activity android:name=".AssignJob" />
        <activity android:name=".EditEmployee" />
        <activity android:name=".OrderDetails" />
    </application>

</manifest>